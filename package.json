{
  "name": "koa-s3-server",
  "version": "0.0.1",
  "description": "Koa server streaming through S3",
  "main": "lib/index.js",
  "scripts": {
    "start": "npm run build:live",
    "test": "jest",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "author": "Lonny Gomes",
  "license": "ISC",
  "devDependencies": {
    "@types/koa": "^2.0.49",
    "@types/node": "^12.6.2",
    "nodemon": "^1.19.1",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  }
}
